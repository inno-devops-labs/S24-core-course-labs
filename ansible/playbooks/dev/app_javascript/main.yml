- name: Install docker
  hosts: all
  roles:
    - role: docker
      become: true

- name: Deploy JavaScript Image
  hosts: all
  roles: 
    - role: web_app
      become: true
      vars:
        app_name: "app_javascript_image"
        app_port: 3000
        app_dir: "/home/{{ ansible_user }}/{{ app_name }}"
        host_name: "{{ ansible_host }}"
        web_app_full_wipe: false