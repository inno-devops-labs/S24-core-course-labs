---
- name: Deploy Docker
  hosts: localhost
  become: yes
  roles:
    - role: ../../roles/docker
    - role: ../../roles/web_app
      vars:
        app_directory: app_python
        app_image: aximaxxi/moscow-time
        app_port: 5000
        wipe_web_app: false
