# Configuration of Docker Role

## Installation of Docker and docker-compose on an Ansible host
Prerequisites:
- Python version 3.8 or higher
- Ubuntu version 22.04
## It is advisable to utilize the apt package manager.