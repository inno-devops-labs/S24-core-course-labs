---
- name: Check {{ ansible_user }} permissions
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true
  tags: docker
  listen: execute_docker

- name: Run docker
  systemd:
    name: docker
    state: restarted
  tags: docker
  delay: 10
  listen: execute_docker