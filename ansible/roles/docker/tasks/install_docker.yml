- name: Install Docker
  apt:
    name: docker-ce
    state: present
  when: ansible_facts['distribution'] == "Ubuntu"

- name: Install Docker Compose
  apt:
    name: docker-compose
    state: present
  when: ansible_facts['distribution'] == "Ubuntu"
