- name: install pip
  ansible.builtin.apt:
    name: python3-pip
    state: present

- name: install docker 
  import_tasks: install-docker.yml
  tags: docker

- name: install docker-compose
  import_tasks: install-compose.yml
  tags: docker
