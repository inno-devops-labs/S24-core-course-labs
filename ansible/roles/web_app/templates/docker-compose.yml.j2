version: "3.7"

services:
  caddy:
    image: "{{ server_image }}"
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - $PWD/index.html:/usr/share/caddy/index.html
