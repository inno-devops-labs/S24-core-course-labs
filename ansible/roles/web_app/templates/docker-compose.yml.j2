version: "3"

services:
  "{{ web_app_name }}":
    image: "{{ web_app_image }}"
    container_name: "{{ web_app_name }}"
    restart: always
    ports:
      - "{{ web_app_port }}:{{ web_container_port }}"
    volumes:
      - {{ data_volume }}:{{ container_dir }}
