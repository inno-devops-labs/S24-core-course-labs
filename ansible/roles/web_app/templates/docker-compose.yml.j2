version: "3"

services:
  {{ web_app_service }}:
    image: {{ docker_image.user }}/{{ docker_image.image_name }}:{{ docker_image.tag }}
    container_name: {{ web_app_service }}
    ports:
      - "{{ ports.external }}:{{ ports.internal }}"
    restart: always
