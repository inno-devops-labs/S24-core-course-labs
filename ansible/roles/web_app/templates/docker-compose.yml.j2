version: "3.9"

services:
  web:
    image: "{{ web_app_docker_image }}:{{ web_app_docker_image_tag }}"
    ports:
      - target: "{{ web_app_target_port }}"
        published: "{{ web_app_published_port }}"
