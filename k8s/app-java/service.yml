apiVersion: v1
kind: Service
metadata:
  name: app-java-service
spec:
  selector:
    app: app-java
  type: LoadBalancer
  ports:
  - name: name-of-service-port
    protocol: TCP
    port: 8080
    targetPort: http-web-svc