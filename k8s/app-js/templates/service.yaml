apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-js.fullname" . }}
  labels:
    {{- include "app-js.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-js.selectorLabels" . | nindent 4 }}
