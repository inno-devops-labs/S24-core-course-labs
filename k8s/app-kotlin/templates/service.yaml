apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-kotlin.fullname" . }}
  labels:
    {{- include "labelslib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "labelslib.selectorLabels" . | nindent 4 }}
