apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-kt-helm.fullname" . }}
  labels:
    {{- include "app-kt-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-kt-helm.selectorLabels" . | nindent 4 }}
