apiVersion: v1
kind: Secret
metadata:
  name: my-secret
  labels:
    app: app-python
    chart: '{{ .Chart.Name }}'
    release: '{{ .Release.Name }}'
    heritage: '{{ .Release.Service }}'
type: Opaque
data:
  password: {{ .Values.password | b64enc | quote }}