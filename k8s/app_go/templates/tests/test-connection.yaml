apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app_go.fullname" . }}-test-connection"
  labels:
    {{- include "library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app_go.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
