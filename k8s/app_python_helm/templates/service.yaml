apiVersion: v1
kind: Service
metadata:
  name: {{ include "app_python_helm.fullname" . }}
  labels:
    {{- include "app_python_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app_python_helm.selectorLabels" . | nindent 4 }}
