apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flutter-chart.fullname" . }}-test-connection"
  labels:
    {{- include "flutter-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flutter-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
