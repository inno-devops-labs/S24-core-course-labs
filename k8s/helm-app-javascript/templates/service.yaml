apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-app-javascript.fullname" . }}
  labels:
    {{- include "helm-app-javascript.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "library-chart.labels" . | nindent 4 }}
