apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "help_app_python.fullname" . }}-test-connection"
  labels:
    {{- include "help_app_python.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "help_app_python.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
