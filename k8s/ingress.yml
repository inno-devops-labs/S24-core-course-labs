apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: python.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moscow-time-app-service
                port:
                  number: 8000
    - host: javascript.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moscow-time-app-js-service
                port:
                  number: 3000
