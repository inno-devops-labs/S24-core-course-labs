apiVersion: v1
kind: Service
metadata:
  name: {{ include "init-container-demo.fullname" . }}
  labels:
    {{- include "init-container-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "init-container-demo.selectorLabels" . | nindent 4 }}
