apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-javascript.fullname" . }}
  labels:
    {{- include "app-javascript.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-javascript.selectorLabels" . | nindent 4 }}
