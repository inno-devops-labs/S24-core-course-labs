apiVersion: v1
kind: Pod
metadata:
  name: pod-install-hook
  annotations:
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
    - name: pod-install-container
      image: busybox
      args:
        - sleep
        - "20"
  restartPolicy: Never
