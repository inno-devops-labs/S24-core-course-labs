apiVersion: v1
kind: Service
metadata:
  name: {{ include "moscowtime-web.fullname" . }}
  labels:
    {{- include "moscowtime-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moscowtime-web.selectorLabels" . | nindent 4 }}
