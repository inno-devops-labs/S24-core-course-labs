apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscowtime-web.fullname" . }}-test-connection"
  labels:
    {{- include "moscowtime-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscowtime-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
