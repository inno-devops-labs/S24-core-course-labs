apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rust-app.fullname" . }}-test-connection"
  labels:
    {{- include "rust-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rust-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
