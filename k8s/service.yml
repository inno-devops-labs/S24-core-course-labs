apiVersion: v1
kind: Service
metadata:
  name: app-python-service
  labels:
    app: app-python
spec:
  selector:
    app: app-python
  type: NodePort
  ports:
    - name: app-python-port
      port: 8000
      targetPort: 8000
