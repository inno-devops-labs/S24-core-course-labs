apiVersion: v1
kind: Service
metadata:
  name: app-python-service
  labels:
    app: app-python
spec:
  type: LoadBalancer
  ports:
    - port: 6000
      targetPort: 6000
  selector:
    app: app-python
  
  
