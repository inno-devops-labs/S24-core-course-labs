apiVersion: v1
kind: Service
metadata:
  name: {{ include "time-web.fullname" . }}
  labels:
    {{- include "time-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "time-web.selectorLabels" . | nindent 4 }}
