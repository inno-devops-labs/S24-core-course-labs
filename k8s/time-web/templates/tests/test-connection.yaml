apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "time-web.fullname" . }}-test-connection"
  labels:
    {{- include "time-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "time-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
