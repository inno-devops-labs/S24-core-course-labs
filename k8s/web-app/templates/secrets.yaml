
apiVersion: v1
kind: Secret
metadata:
  name: credentials
  labels:
    app: web-app
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  MYPASSWORD: "{{ .Values.MYPASSWORD | toString | b64enc }}"