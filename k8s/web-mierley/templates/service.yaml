apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-mierley.fullname" . }}
  labels:
    {{- include "web-mierley.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-mierley.selectorLabels" . | nindent 4 }}
